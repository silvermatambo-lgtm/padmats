import {MessageCircle} from 'lucide-react'; import {useState} from 'react';
export default function FloatingWhatsApp(){const[o,setO]=useState(false);return <div className="fixed right-5 bottom-24 z-50"><button onClick={()=>setO(!o)} className="w-14 h-14 rounded-full bg-[#496b35] text-white shadow-xl grid place-items-center"><MessageCircle/></button>{o&&<div className="absolute bottom-16 right-0 w-72 bg-white rounded-2xl shadow-2xl p-5 border"><b className="text-[#07344a]">Chat with Padmaats</b><p className="text-sm text-slate-600 mt-2">WhatsApp integration is part of Phase 1. The final Padmaats WhatsApp number still needs to be supplied before this button can open WhatsApp.</p><a href="mailto:info@padmaats.co.za" className="pad-btn mt-4 !py-2 !px-4 text-sm">Email instead</a></div>}</div>}

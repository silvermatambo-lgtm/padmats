import {MessageCircle,CalendarDays,Send,X} from 'lucide-react'; import {useState} from 'react';
export default function FloatingWhatsApp(){const[o,setO]=useState(false);const[step,setStep]=useState(1);const[name,setName]=useState('');const[type,setType]=useState('General travel enquiry');const[from,setFrom]=useState('');const[to,setTo]=useState('');const[msg,setMsg]=useState('');
const submit=()=>{setStep(3)};
return <div className="fixed right-5 bottom-24 z-50"><button onClick={()=>setO(!o)} className="w-14 h-14 rounded-full bg-[#25D366] text-white shadow-xl grid place-items-center"><MessageCircle/></button>{o&&<div className="absolute bottom-16 right-0 w-[340px] max-w-[90vw] bg-white rounded-2xl shadow-2xl border overflow-hidden"><div className="bg-[#07344a] text-white p-4 flex justify-between"><div><b>Padmaats Travel Assistant</b><p className="text-xs text-white/70">Plan an enquiry in a few steps</p></div><button onClick={()=>setO(false)}><X size={18}/></button></div><div className="p-4">
{step===1&&<><label className="text-sm font-semibold text-[#07344a]">Your name</label><input value={name} onChange={e=>setName(e.target.value)} className="form-field mt-1 mb-3" placeholder="Name"/><label className="text-sm font-semibold text-[#07344a]">What can we help with?</label><select value={type} onChange={e=>setType(e.target.value)} className="form-field mt-1 mb-3"><option>General travel enquiry</option><option>Namibia self-drive</option><option>Stay in Namibia</option><option>Car Hire / 4x4</option><option>Padmaats Journey</option><option>Seven Churches 2027</option></select><button onClick={()=>setStep(2)} className="pad-btn !py-2.5 !px-4 w-full justify-center">Continue</button></>}
{step===2&&<><div className="flex items-center gap-2 text-[#07344a] font-semibold mb-3"><CalendarDays size={18}/> Preferred travel dates</div><div className="grid grid-cols-2 gap-2"><input type="date" value={from} onChange={e=>setFrom(e.target.value)} className="form-field"/><input type="date" value={to} onChange={e=>setTo(e.target.value)} className="form-field"/></div><textarea value={msg} onChange={e=>setMsg(e.target.value)} className="form-field mt-3 min-h-24" placeholder="Tell us what you are planning..."/><button onClick={submit} className="pad-btn !py-2.5 !px-4 w-full justify-center mt-3"><Send size={16}/> Submit enquiry</button></>}
{step===3&&<div className="text-center py-3"><div className="text-3xl mb-2">✓</div><b className="text-[#07344a]">Enquiry captured</b><p className="text-sm text-slate-600 mt-2">Thanks {name||'there'}. Your {type.toLowerCase()} request for {from||'your selected dates'}{to?' to '+to:''} is ready for the final WhatsApp/email connection.</p><a href="mailto:info@padmaats.co.za" className="pad-btn mt-4 !py-2 !px-4 text-sm">Email Padmaats</a></div>}
</div></div>}</div>}